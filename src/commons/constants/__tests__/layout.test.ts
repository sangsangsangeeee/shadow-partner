import { CHIP_TRAY_H, TOAST_MS } from '../layout';

/**
 * 기획서가 숫자를 직접 정한 자리들.
 *
 * 다른 테스트는 이 상수를 가져다 쓰기 때문에 값이 바뀌어도 같이 따라가서 통과한다.
 * 그래서 값 자체를 못 박는 자리가 따로 필요하다 —
 * 여기가 깨지면 기획서(9장 삭제와 되돌리기)를 먼저 고쳤는지 확인해라.
 */
describe('기획서가 정한 치수', () => {
  it('토스트는 3초 뒤 사라진다', () => {
    expect(TOAST_MS).toBe(3000);
  });

  it('칩 상자는 두 줄분이다', () => {
    // 칩 한 줄 33(글자 21 + 세로 여백 6×2) × 2 + 줄 사이 4 + 상자 안쪽 여백 12×2
    expect(CHIP_TRAY_H).toBe(94);
  });
});
